apiVersion: v1
kind: Service
metadata:
  name: neo4j-service
  labels:
    app: neo4j-service
spec:
  selector:
    app: neo4j-service
  ports:
  - name: http
    protocol: TCP
    port: 8003
    targetPort: 8003
  type: ClusterIP